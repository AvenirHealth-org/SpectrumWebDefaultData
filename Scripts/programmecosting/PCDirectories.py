import os

from SpectrumCommon.Const.PC.PCConst import *

def create_PC_directories():
    pass
    
    # Currently, none of the PC datbases are by year.  If they were, put them here.  

    # PC_path = os.getcwd()+'\Tools\DefaultDataManager\PC\\'
    # dir_list = [PC_path+ 'JSON\\' + PC_XXX_DB_DIR ,
    #             ]
    # for path in dir_list:
    #     os.makedirs(path, exist_ok=True)